<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ view_model.page.title | default: "Welcome" }}</title>
<body>
    <main>
        <section>
            <h2>{{ view_model.page.heading | default: "Latest Posts" }}</h2>
            {% if posts %}
                <ul>
                    {% for post in posts limit: 5 %}
                        <li>
                            <a href="{{ post.url }}">{{ post.title }}
                            <span class="date">{{ post.date | date: "%B %d, %Y" }}</span>
                        </li>
                    {% endfor %}
                </ul>
            {% else %}
                <p>No posts available.
            {% endif %}
        </section>
    </main>

    <footer>
        <p>&copy; {{ "now" | date: "%Y" }} {{ site.name | default: "My Website" }}. All rights reserved.</p>
